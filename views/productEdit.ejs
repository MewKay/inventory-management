<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <section>
      <form action="<%= product.id %>/delete" method="post">
        <button
          type="submit"
          onclick="return confirm('Are you sure you want to delete ?')"
        >
          Delete
        </button>
      </form>
      <form action="/edit/product/<%= product.id %>">
        <%- include("partials/productForm.ejs", { product, categories }) %>
        <div>
          <a href="/view/products/<%= product.id %>">Return</a>
          <button type="submit">
            Save
          </button>
        </div>
      </form>
    </section>
  </body>
</html>
