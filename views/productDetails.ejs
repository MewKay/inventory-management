<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head.ejs") %>
    <link rel="stylesheet" href="/styles/productDetails.css" />
    <title><%= product.name %></title>
  </head>
  <body>
    <%- include("partials/header.ejs") %>
    <main>
      <article class="details-container">
        <div class="heading">
          <h3><%= product.name %></h3>
          <p><%= product.category %></p>
        </div>
        <div class="product-details">
          <p>
            <em><%= product.quantity %> <%= product.unit %></em> left in stock
          </p>
          <p><em>$<%= product.price %></em> per unit</p>
          <p><em>$<%= product.total_stock_price %></em> total stock price</p>
        </div>
        <div class="link-container">
          <a class="edit-link" href="/edit/products/<%= product.id %>">
            Edit Product
          </a>
        </div>
      </article>
    </main>
  </body>
</html>
