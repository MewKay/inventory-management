<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <section>
      <header>
        <h3>Edit Product</h3>

        <form action="<%= product.id %>/delete" method="post">
          <button
            type="submit"
            onclick="return confirm('Are you sure you want to delete ?')"
          >
            Delete
          </button>
        </form>
      </header>
      
      <form action="/edit/products/<%= product.id %>" method="post">
        <%- include("partials/productForm.ejs", { product, categories, errors }) %>
        <div>
          <a href="/view/products/<%= product.id %>">Return</a>
          <button type="submit">
            Save
          </button>
        </div>
      </form>
    </section>
  </body>
</html>
